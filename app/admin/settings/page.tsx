import SettingsForm from '@/components/admin/SettingsForm'; import { requireRole } from '@/lib/auth/server'; import { createClient } from '@/lib/supabase/server'; import type { SchoolSettings } from '@/types/database'
export const dynamic = 'force-dynamic'
export default async function Page() { await requireRole('admin'); const { data, error } = await (await createClient()).from('school_settings').select('*').limit(1).maybeSingle(); return <div className="space-y-6"><header><p className="text-sm font-semibold text-primary-700">Pengaturan</p><h1 className="text-3xl font-bold">Konfigurasi Sekolah</h1><p className="mt-2 text-sm text-slate-600">Satu sumber identitas sekolah untuk dokumen akademik.</p></header>{error ? <p role="alert" className="rounded-xl bg-red-50 p-4 text-red-800">Pengaturan tidak dapat dimuat.</p> : <SettingsForm settings={(data as SchoolSettings | null) ?? null} />}</div> }
